"use client";



const page = () => {
  const sendMessage = () => {
  };
  return (
    <div>
      <input placeholder="Message" />
      <button onClick={sendMessage}>Send Message</button>
    </div>
  );
};

export default page;
